# Indexer

Home for replay, materialization, and query-oriented derived state logic.

The first executable slice materializes agent history, checkpoint lineage,
migration lineage, and current agent state from repository-local event storage.
