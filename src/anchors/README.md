# Anchors

This package exports stable anchor records for high-value Continuum state.

The v0 bootstrap keeps the adapter local and replaceable:

- core logic computes canonical roots
- the adapter records export metadata
- no external chain dependency is required for repository reconstruction
