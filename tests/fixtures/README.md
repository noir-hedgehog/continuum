# Fixtures

This directory stores replayable scenario inputs for the repository-local Continuum prototype.

Current fixture surfaces:

- `continuum_demo_v0.json` defines the minimum Gate 5 demo expectations for fresh-session reconstruction, continuity assessment, governance replay, and anchor export.
